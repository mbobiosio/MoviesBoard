<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cerminnovations.moviesboard">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.cerminnovations.moviesboard.MovieApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:fullBackupContent="false"
        android:theme="@style/Theme.MoviesBoard">
        <activity android:name=".ui.activity.SearchActivity"/>
        <activity android:name=".ui.activity.ArtistCastsActivity" />
        <activity android:name=".ui.activity.ArtistDetailsActivity" />
        <activity android:name=".ui.activity.AllSeriesActivity" />
        <activity android:name=".ui.activity.SeriesDetailsActivity" />
        <activity android:name=".ui.activity.MovieDetailActivity" />
        <activity android:name=".ui.activity.AllMoviesActivity" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>