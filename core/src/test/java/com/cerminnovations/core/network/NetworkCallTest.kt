package com.cerminnovations.core.network

import kotlinx.coroutines.ExperimentalCoroutinesApi
import kotlinx.coroutines.test.runTest
import org.junit.Test

/**
 * @Author Mbuodile Obiosio
 * https://linktr.ee/mbobiosio
 */
@ExperimentalCoroutinesApi
class NetworkCallTest {

    // private val dispatcher = TestCoroutineDispatcher()
    // private val moshi = Moshi.Builder().build()
    // private val adapter: JsonAdapter<ErrorMessage> = moshi.adapter(ErrorMessage::class.java)

    @Test
    fun `when lambda returns successfully then it should emit the result as success`() {
        runTest {
            // val lambdaResult = true
            // val result = call
        }
    }
}
